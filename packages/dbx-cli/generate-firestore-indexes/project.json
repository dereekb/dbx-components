{
  "name": "dbx-cli-generate-firestore-indexes",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/dbx-cli/generate-firestore-indexes/src",
  "projectType": "application",
  "tags": [],
  "implicitDependencies": [],
  "targets": {
    "build-base": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{workspaceRoot}/dist/packages/dbx-cli/generate-firestore-indexes"],
      "defaultConfiguration": "production",
      "dependsOn": [
        {
          "projects": ["dbx-cli"],
          "target": "build-base"
        }
      ],
      "metadata": {
        "description": "An explicit edge to dbx-cli:build-base, NOT `^build`. This entry point compiles into the parent's dist directory, which `dbx-cli:build-base` deletes and rewrites (rollup `deleteOutputPath: true`), so it cannot run before the parent -- but the edge has to be spelled out, because `^build` here would resolve to dbx-cli:build, which depends on this very target: a hard cycle. That XOR is the whole design constraint -- either the children get `^build` or the parent aggregates them, never both. Aggregating is the side that keeps one writer per dist directory.  Its other dependency, dbx-cli-firestore-indexes, is a source-only sibling with no build target of any kind -- it is compiled straight into this bundle, so there is nothing to order against."
      },
      "options": {
        "outputPath": "dist/packages/dbx-cli/generate-firestore-indexes",
        "main": "packages/dbx-cli/generate-firestore-indexes/src/main.ts",
        "tsConfig": "packages/dbx-cli/generate-firestore-indexes/tsconfig.tool.json",
        "format": ["esm"],
        "platform": "node",
        "target": "node20",
        "bundle": true,
        "thirdParty": false,
        "skipTypeCheck": false,
        "declaration": false,
        "esbuildOptions": {
          "banner": {
            "js": "#!/usr/bin/env node\nimport { createRequire as __createRequire } from 'node:module';\nconst require = __createRequire(import.meta.url);"
          }
        }
      },
      "configurations": {
        "production": {
          "minify": false,
          "sourcemap": false
        }
      }
    },
    "regenerate": {
      "executor": "nx:run-commands",
      "dependsOn": ["build-base"],
      "options": {
        "command": "node dist/packages/dbx-cli/generate-firestore-indexes/main.js --component packages/firebase --component packages/openrouter/firebase --component components/demo-firebase",
        "cwd": "{workspaceRoot}",
        "color": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    }
  }
}
