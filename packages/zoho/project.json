{
  "name": "zoho",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "components/zoho/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/zoho"],
      "options": {
        "commands": [
          {
            "command": "npx nx run zoho:build-base"
          },
          {
            "command": "npx nx run zoho-nestjs:build-base"
          }
        ],
        "parallel": false
      }
    },
    "build-base": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/zoho",
        "tsConfig": "components/zoho/tsconfig.lib.json",
        "packageJson": "components/zoho/package.json",
        "main": "components/zoho/src/index.ts",
        "assets": ["components/zoho/*.md"]
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["components/zoho/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/packages/zoho"],
      "options": {
        "jestConfig": "components/zoho/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
