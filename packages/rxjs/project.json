{
  "name": "rxjs",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/rxjs/src",
  "projectType": "library",
  "tags": ["publishable"],
  "implicitDependencies": ["!util-test"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/rxjs"],
      "options": {
        "color": true,
        "commands": [
          {
            "command": "npx nx run rxjs:build-base"
          },
          {
            "description": "normalize + verify the ESM-only package shape",
            "command": "node tools/scripts/finalize-esm-exports.mjs dist/packages/rxjs"
          }
        ],
        "parallel": false
      },
      "dependsOn": ["^build"]
    },
    "build-base": {
      "dependsOn": []
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "ci-publish-npmjs": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd ./dist/packages/rxjs && npm publish --access public"
      }
    }
  }
}
