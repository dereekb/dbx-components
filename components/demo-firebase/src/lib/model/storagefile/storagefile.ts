import { UploadedFileTypeIdentifier } from '@dereekb/firebase';

// MARK: Test File
/**
 * A test file that is uploaded by a user into their own uploads folder.
 */
export const TEST_UPLOAD_FILE_TYPE: UploadedFileTypeIdentifier = 'TEST';
